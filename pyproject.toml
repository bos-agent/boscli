[project]
name = "boscli"
version = "1.6.0"
description = "AWS BOS AI CLI"
dependencies = [
    "bos-ai == 1.6.0",
]

[project.scripts]
boscli = "bos.cli.entry:main"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
